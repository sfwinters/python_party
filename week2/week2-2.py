# set 
# unordered list of just keys, no values - cannot repeat key name, otherwise not super useful
# will probably need it for the exam
example_set = {"key", "key1", "key2"}
print(example_set)
del example_set # This will unassign the variable name, but the set will still exist

# In Terminal, you can type "python" (or "py" or "python3") to open a shell and script line-by-line
# Or you can open IDLE - opens a python shell (Mac/Windows only, not on Linux)




